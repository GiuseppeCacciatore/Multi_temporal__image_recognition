pre-requisites on OS:
we used: Ubuntu 14.04 LTS - 64 bit

software/libraries required:
- Java default libraries
- Android Studio 1.0, we have debug with "Samsung S4".

Api version that we have used in build.gradle file:

 minSdkVersion 14
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"

Note:
all photos are saved in SDCard of the smartphone in the folder "SlideshowRicevuto"
